#get a bank account
query GET_BANK_ACCOUNT ($ID: ID!) {
    bankAccount(id: $ID) {
        id
        name
        currency
        client {
            id
            firstName
            lastName
        }
    }
}